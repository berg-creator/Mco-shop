# Mco shop

Telegram-бот магазина одежды с мини-приложением: витрина с актуальным наличием,
категории, фильтры, корзина. Заказ приходит владельцу в личку заявкой с кнопками
«Подтвердить» и «Отказать».

## Что умеет

- **Витрина внутри Telegram.** Категории (куртки, штаны, кофты, обувь,
  аксессуары — список правится), фильтры по бренду, размеру, состоянию и цене,
  поиск, карточка с фото и описанием.
- **Наличие по размерам.** Проданный размер исчезает из витрины сам, вещь без
  единого размера в наличии уходит из каталога. Вручную ничего скрывать не надо.
- **Резерв, а не гонка.** Пока владелец не ответил на заявку, вещь считается
  занятой: двое не купят одну куртку. Отказ возвращает её в витрину.
- **Админка в боте.** Товар заводится с телефона: фото, название, бренд,
  категория, цена, размеры. Остатки правятся кнопкой.
- **Связь с продавцом.** Кнопка в витрине ведёт в чат с ботом: вопрос уходит
  владельцу пересылкой, ответ возвращается покупателю от имени магазина —
  личный аккаунт владельца при этом остаётся его личным.
- **Импорт из канала.** Архив постов магазина разбирается в карточки товаров.

Чего пока нет: онлайн-оплаты (заявка → переписка → оплата как договоритесь),
доставки через API служб, статистики продаж. Это следующие шаги, а не забытое.

## Быстрый старт

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
cp .env.example .env               # вписать токен бота и свой Telegram id
.venv/bin/python -m src.db --init --demo   # база и десяток демо-товаров
```

Посмотреть витрину в браузере, без бота и токена:

```bash
.venv/bin/python -m src.app --web-only     # http://127.0.0.1:8080/app/
```

## Как запустить по-настоящему

Telegram пускает в мини-приложение только по HTTPS, поэтому локальному
магазину нужен туннель — он выдаёт временный адрес с сертификатом. Адрес этот
меняется при каждом запуске, и вписывать его в `.env` руками приходилось бы
каждый раз, поэтому есть одна команда на всё:

```bash
./run.sh
```

Скрипт останавливает прежний магазин на порту, поднимает туннель, подставляет
свежий адрес витрины (и в `.env`, чтобы `--check` показывал правду), запускает
бота и веб. Ctrl+C гасит и туннель, и магазин.

```
поднимаю туннель..
витрина: https://alhqb-79-127-249-84.free.pinggy.net/app/
версия:  00f5483
```

Туннель поднимается через [pinggy](https://pinggy.io) по обычному ssh на 443:
`cloudflared` требует исходящий порт 7844, а в домашних сетях он часто закрыт —
там витрина снаружи отвечает ошибкой 1033. Бесплатный туннель живёт час, потом
`./run.sh` заново. Это режим «показать и проверить», а не продавать: для
круглосуточной работы нужен постоянный домен.

Если адрес витрины постоянный (свой домен или VPS), туннель не нужен вовсе —
`WEBAPP_URL` в `.env` правится один раз, и магазин запускается напрямую:

```bash
.venv/bin/python -m src.app
```

Что нужно завести один раз:

1. **Бот** — [@BotFather](https://t.me/BotFather) → `/newbot` → токен
   в `TELEGRAM_BOT_TOKEN`.
2. **Свой id** — [@userinfobot](https://t.me/userinfobot) → число в `ADMIN_IDS`.
   Через запятую можно указать двоих: владельца и помощника.

## Каталог

**С телефона.** В боте: `/add` — мастер из семи шагов, `/items` — список
товаров с кнопками «Остаток», «Скрыть», «Удалить», `/orders` — последние заявки.
`/music` — прислать боту свою песню: витрина заиграет её, а фото в карточке
начнут листаться в её темпе. Темп бот меряет сам, по самому файлу.

**Из публичного канала.** Самый быстрый способ поднять витрину: сборщик читает
веб-версию канала и раскладывает посты по карточкам.

```bash
.venv/bin/python -m src.scrape Mcoworldwide --out draft.json
.venv/bin/python -m src.importer --apply draft.json
```

Разбор сделан под формат карточек этого магазина: название, `Размер:`
или `Размеры(EUR):`, `Состояние:`, `Стоимость:`. Повтор размера в посте
(«41, 41, 42») считается двумя парами. Наличие берётся из ответов на пост:
«❗️42 ПРОДАНО❗️» убирает одну пару, «❗️ПРОДАНО❗️» закрывает вещь,
«8.000₽ — новая цена» ставит скидку со старой ценой зачёркнутой.
Посты без размера — «можем привезти под заказ» — пропускаются и выводятся
отдельным списком, их заводят руками, если нужно.

**Из экспорта канала** (если канал закрытый). Telegram Desktop → Настройки →
Экспорт данных → канал магазина, формат JSON, с фотографиями. Дальше:

```bash
.venv/bin/python -m src.importer ~/export/result.json --out draft.json
```

Разбор эвристический: цена, размеры, бренд и категория угадываются из текста
поста, посты без цены пропускаются, помеченные «продано» не заливаются.
Поэтому шага два: сначала черновик `draft.json`, который нужно просмотреть
глазами и поправить, потом заливка:

```bash
.venv/bin/python -m src.importer --apply draft.json
```

## Заявки

Покупатель оформляет заказ в витрине → заявка падает владельцу в личку:
кто, телефон, доставка, позиции, сумма. Кнопка «Подтвердить» списывает остаток
и пишет покупателю, что вещи отложены. «Отказать» снимает резерв и возвращает
вещь в витрину. Кнопка «Написать покупателю» открывает с ним диалог.

## Вопросы покупателей

Всё, что покупатель пишет боту, пересылается владельцу, а покупатель видит
«Передал владельцу магазина». Владелец отвечает **ответом (reply)** на
пересланное сообщение — бот доставит ответ покупателю от имени магазина.
Если у покупателя закрыты пересылки, бот скажет об этом: такому можно
написать только кнопкой «Написать покупателю» под его заявкой.

## Переезд на VPS

Локальный запуск — для проверки. Чтобы магазин работал круглосуточно, нужен
хостинг с постоянным адресом (Timeweb, Amvera, Beget — от 200 ₽ в месяц):

1. `git clone`, `python3 -m venv .venv`, `pip install -r requirements.txt`.
2. `.env` с постоянным `WEBAPP_URL` (домен с сертификатом, например через
   nginx + Let's Encrypt или встроенный HTTPS хостинга).
3. Юнит systemd с `ExecStart=/путь/.venv/bin/python -m src.app` и
   `Restart=always`.
4. Резервная копия `data/` — там весь каталог, фото и история заказов.

Код при этом не меняется: тот же процесс, тот же long polling.

## Что сервер делает без человека

Всё, что можно поручить машине, поручено машине — `scripts/deploy.sh`
разворачивает это вместе с магазином:

- **Магазин падает** — systemd поднимает его сам (`Restart=always`). Если
  не поднялся совсем, владельцу приходит сообщение в Telegram.
- **Копия базы** — каждую ночь в `backups/`, с проверкой `integrity_check`:
  непроверенная копия выглядит как удачная ночь ровно до дня, когда она
  понадобится. Хранится две недели. По понедельникам копия уезжает владельцу
  в Telegram — вторая площадка бесплатно, копия рядом с оригиналом не спасает
  от смерти диска.
- **Сторож** (`shop-watch`, раз в четверть часа) смотрит на то, что ломается
  тихо, и сначала лечит сам: копии нет — делает, места мало — чистит журнал
  и кеш пакетов, витрина не отвечает — перезапускает магазин (не чаще раза
  в час: вторая попытка не поможет, зато скроет, что магазин лежит),
  сертификат кончается — продлевает. Не вылечилось — сообщение владельцу
  с хвостом журнала, чтобы разбор начинался с телефона, а не с ssh.
  Лечится только то, что уже случалось: сторож, который чинит непонятое,
  из мелкой поломки делает потерю данных. Музыку, фото и старые копии
  он не трогает — что из них лишнее, решает владелец.
  Пишет только при смене состояния: одинаковая тревога каждые пятнадцать
  минут перестаёт читаться, и настоящую пропускают вместе с ней.
- **Обновления безопасности** ставятся сами, перезагрузка — в пять утра:
  до перезагрузки заплатка на ядро не работает, а вручную «когда-нибудь»
  не случается никогда.
- **Сертификат** продлевает `certbot.timer`, журнал ограничен 200 МБ.

Сообщения шлёт бот магазина: токен и `ADMIN_IDS` уже есть в `.env`, второй
канал заводить незачем. Проверить связь: `shop-alert "проверка"` на сервере.

На человеке остаётся то, что машине поручить нельзя: чинить сломавшееся
впервые, решать «стереть музыку или доплатить за диск» и развивать магазин.

## Проверка

```bash
.venv/bin/python -m src.app --check          # настройки и база
.venv/bin/python -m src.db --check           # что лежит в каталоге
.venv/bin/python -m unittest discover tests  # весь бот: 232 теста
```

Тесты покрывают `src/` целиком — каждую строку и каждую ветку. Замерить:

```bash
.venv/bin/pip install coverage               # только для разработки
.venv/bin/python -m coverage run -m unittest discover tests
.venv/bin/python -m coverage report -m       # планка 100% в .coveragerc
```

Витрина (`webapp/`) проверяется глазами: `--web-only` и браузер.

Правила проекта и объяснение решений — в [CLAUDE.md](CLAUDE.md).
